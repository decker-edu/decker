Fonts for MathJax
-----------------

To get the current version of these fonts, do
```
rm -rf ./node_modules ./package-lock.json
npm install
```

Make sure that the version matches the MathJax installation (see package-lock.json after installation).